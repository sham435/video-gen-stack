node_modules/
.env
data/
# Root runtime storage (renders/news/audio) — packages/storage sources stay tracked
/storage/
snapshots/
!snapshots/layout-snapshots.json
.DS_Store
*.log
Library/
.virtualenvs/
output/
scripts/get-youtube-token.mjs

# Agent-local config/memory — never commit real files; use the sanitized example
.opencode/opencode.json
.opencode-memory.json

# audio cache + py
assets/music/normalized/
scripts/__pycache__/

# Scratch / generated output — never track. Already-tracked files (e.g. the
# 28 production/runs publication.json) stay visible; only untracked noise is
# silenced.
session-ses_*.md
.newsmonster/
production/runs/
